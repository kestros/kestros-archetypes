<archetype-descriptor name="tutorial-application-archetype"
  xmlns="http://maven.apache.org/plugins/maven-archetype-plugin/archetype/1.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/plugins/maven-archetype-plugin/archetype/1.0.0 http://maven.apache.org/xsd/archetype-1.0.0.xsd">

  <id>kestros-tutorial-application-archetype</id>

  <allowPartial>true</allowPartial>

  <fileSets>
    <fileSet filtered="true" packaged="false">
      <directory/>
      <includes>
        <include>gitignore</include>
      </includes>
    </fileSet>

    <fileSet filtered="true" packaged="false">
      <directory/>
      <includes>
        <include>pom.xml</include>
      </includes>
    </fileSet>

    <fileSet filtered="true" packaged="false">
      <directory/>
      <includes>
        <include>README.md</include>
      </includes>
    </fileSet>

    <!-- The whole tutorial content package (at src/content, matching the sibling archetypes and the
         filevault plugin's filterSource in the generated pom): twelve tutorial-step UI frameworks, three vendor
         libraries and the component views that go with them. Every file here is text (xml, less,
         html, js) and some carry ${...} placeholders, so the whole tree is filtered. There are no
         binaries in this module - if any are ever added they need their own unfiltered fileSet,
         because filtering corrupts them. -->
    <fileSet filtered="true" packaged="false">
      <directory>src/content</directory>
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>

    <!-- The tutorial's Java, matching kestros-application-archetype. packaged="true" is what puts
         the class under the generated project's package directory instead of at the source root -
         the .java files here declare "package ${package}.application.datasources", so without it
         the package statement and the path disagree and the generated project will not compile. -->
    <fileSet filtered="true" packaged="true">
      <directory>src/main/java</directory>
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>

    <fileSet filtered="true" packaged="true">
      <directory>src/test/java</directory>
      <includes>
        <include>**/*</include>
      </includes>
    </fileSet>
  </fileSets>

  <requiredProperties>
    <requiredProperty key="artifactDescription">
      <defaultValue></defaultValue>
    </requiredProperty>
    <requiredProperty key="artifactName">
      <defaultValue>${artifactId}</defaultValue>
    </requiredProperty>
    <requiredProperty key="artifactIdNoSpecialCharacters">
      <defaultValue>${artifactId}</defaultValue>
    </requiredProperty>
    <requiredProperty key="organizationName">
      <defaultValue></defaultValue>
    </requiredProperty>
    <requiredProperty key="hasParentProject">
      <defaultValue>false</defaultValue>
    </requiredProperty>
    <requiredProperty key="hashSymbol">
      <defaultValue>#</defaultValue>
    </requiredProperty>
    <requiredProperty key="artifactIdShorthand">
      <defaultValue></defaultValue>
    </requiredProperty>
  </requiredProperties>

</archetype-descriptor>
