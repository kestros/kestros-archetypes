<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <!-- No parent, matching kestros-application-archetype and kestros-project-archetype, whose
         parent blocks are likewise commented out. This module previously declared
         kestros-archetypes:0.2.0-SNAPSHOT with relativePath ../pom.xml - but the root pom is
         version 0.9.0, so the relativePath never matched and Maven resolved that parent from the
         private Nexus instead. Three consequences, all removed by dropping it: the whole reactor
         gained a private-repository dependency, a SNAPSHOT parent made the module unreleasable,
         and the licence below was being inherited as GPL-v3.0 from a 2025 parent while this
         project is Apache-2.0. -->

    <groupId>io.kestros.cms</groupId>
    <artifactId>kestros-tutorial-application-archetype</artifactId>
    <version>0.9.0</version>

    <name>Kestros Tutorial Application Module Archetype</name>

    <description>Archetype for generating a guided tutorial project for Kestros.</description>

    <url>https://www.kestros.io</url>

    <licenses>
        <license>
            <name>Apache License, Version 2.0</name>
            <url>https://www.apache.org/licenses/LICENSE-2.0.txt</url>
            <distribution>repo</distribution>
        </license>
    </licenses>

    <packaging>maven-archetype</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <archetype.version>3.2.1</archetype.version>
        <antrun.version>3.1.0</antrun.version>
    </properties>

    <build>
        <!-- Declared here rather than inherited, now that this module has no parent - the same
             shape kestros-application-archetype uses. Without it the packaging above is unknown
             and the module cannot be read at all. -->
        <extensions>
            <extension>
                <groupId>org.apache.maven.archetype</groupId>
                <artifactId>archetype-packaging</artifactId>
                <version>${archetype.version}</version>
            </extension>
        </extensions>
        <plugins>
            <plugin>
                <artifactId>maven-archetype-plugin</artifactId>
                <version>${archetype.version}</version>
            </plugin>
            <!-- Checks the project archetype:integration-test just generated, in the phase after
                 it. Version is explicit because this module has no parent to inherit one from.
                 If a second check is ever added to this module it goes in as another <exec>
                 here, not as a second plugin block. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-antrun-plugin</artifactId>
                <version>${antrun.version}</version>
                <executions>
                    <execution>
                        <id>check-generated-content</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>run</goal>
                        </goals>
                        <configuration>
                            <target>
                                <exec executable="bash" failonerror="true">
                                    <arg value="${project.basedir}/src/test/scripts/check-filter-roots.sh"/>
                                    <arg value="${project.basedir}"/>
                                </exec>
                                <exec executable="bash" failonerror="true">
                                    <arg value="${project.basedir}/src/test/scripts/check-tutorial-content.sh"/>
                                    <arg value="${project.basedir}"/>
                                </exec>
                            </target>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Regenerates src/test/resources/projects/it1/reference from the project the archetype
             integration test just generated. The reference tree is 244 files and every one of
             them is output, so it is rebuilt rather than edited:

                 mvn -B verify -Pregenerate-reference

             Run it after any change to archetype-resources, read the resulting git diff to check
             the change is the one you meant, and commit the tree with the change. Editing a
             reference file by hand makes the comparison agree with a project the archetype does
             not actually produce, which is worse than having no comparison. -->
        <profile>
            <id>regenerate-reference</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-antrun-plugin</artifactId>
                        <version>${antrun.version}</version>
                        <executions>
                            <execution>
                                <id>regenerate-reference</id>
                                <phase>verify</phase>
                                <goals>
                                    <goal>run</goal>
                                </goals>
                                <configuration>
                                    <target>
                                        <delete dir="${project.basedir}/src/test/resources/projects/it1/reference"/>
                                        <copy todir="${project.basedir}/src/test/resources/projects/it1/reference">
                                            <fileset dir="${project.build.directory}/test-classes/projects/it1/project/build-integration-test"/>
                                        </copy>
                                    </target>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>