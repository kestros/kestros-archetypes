<?xml version="1.0" encoding="UTF-8"?>
<jcr:root xmlns:jcr="http://www.jcp.org/jcr/1.0" xmlns:kes="http://kestros.io/kes/1.0"
          jcr:primaryType="nt:unstructured"
          jcr:title="${artifactName} Sample Card"
          jcr:description="A single card's title and description built programmatically by this project's SampleCardDataSource. Select this datasource on a Card component (stored on the component instance as the kes:datasource property)."
          group="${artifactName}"
          classPath="${package}.application.datasources.SampleCardDataSource"
          container="{Boolean}true"/>
